
/**
 * Configuração do Firebase.
 * 
 * Para obter estas chaves:
 * 1. Vá para o Console do Firebase (https://console.firebase.google.com/)
 * 2. Selecione seu projeto.
 * 3. Clique no ícone de engrenagem > Configurações do projeto.
 * 4. Em "Seus aplicativos", selecione o app Web (ou crie um) e copie o objeto firebaseConfig.
 */

export const firebaseConfig = {
  apiKey: "env-api-key",
  authDomain: "env-auth-domain",
  projectId: "env-project-id",
  storageBucket: "env-storage-bucket",
  messagingSenderId: "env-messaging-sender-id",
  appId: "env-app-id"
};
